const vetPilha = new Array(1, 2, 3, 4, 5)
const vetAdiciona = new Array(6, 7, 8, 9, 10)
let vetResultante = new Array()

vetResultante = vetResultante.concat(vetPilha,vetAdiciona)

console.log(vetResultante)